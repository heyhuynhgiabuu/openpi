/**
 * IPC domain barrel — re-exports from ipc/ submodules.
 *
 * Currently re-exports from the full preserved source.
 * As sections are extracted into domain files, they are imported
 * here instead of _full to reduce the monolithic file.
 */
export * from './_full'
